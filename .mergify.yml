pull_request_rules:
  - name: Automatic merge on approval
    conditions:
      - "#approved-reviews-by>=1"
      - base=main
      - label=A:Automerge
    actions:
      merge:
        method: squash
        strict: true
        commit_message: title+body
